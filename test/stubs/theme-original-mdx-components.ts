// Stub for Docusaurus' generated `@theme-original/MDXComponents` module, which
// only exists during a real build. Vitest aliases the import here so the
// MDXComponents swizzle can be unit-tested in isolation. Mirrors the shape of
// the real default export (a map of HTML tag -> component) closely enough for
// the swizzle's spread to be exercised.
export default {
  code: () => null,
  a: () => null,
};
